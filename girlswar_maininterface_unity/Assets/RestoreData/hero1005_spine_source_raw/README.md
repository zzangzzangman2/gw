# Hero 1005 Raw Spine Source

These `.skel.bytes` and `.atlas.txt` files were re-extracted from Unity TextAsset `m_Script` with `surrogateescape`.
The older `hero1005_spine_source` files are kept as UI123 evidence; this raw folder is the UI124 Spine import input.

Use `Painting_1005` main-only first for the home lobby SkeletonGraphic mount.
