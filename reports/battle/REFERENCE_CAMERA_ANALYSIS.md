# 참고.mp4 카메라/배치 정밀 분석 (vs 현재)

참고: `C:\Users\godho\Downloads\참고.mp4` = **1280×570, 30fps, 121s**. 전투 ~18~60s.
조밀 프레임: `reports/battle/reference_video/dense/r_*.png`, 콘택트 `REF_DENSE_CONTACT.png`,
비교 `REF_VS_OURS.png`.

## 일치 ✅
- 해상도 1280×570 동일.
- 카메라 각도: ortho 2.5D 정면. 원근감은 맵 아트 + 액터 깊이(뒷줄 위/작게)로. (카메라 회전 없음)
- 배치 구조: 우리팀 좌측 2열 사선, 적 우측. (실제 스테이션 좌표 사용)
- 기본 카메라: 정적.

## 불일치 ❌ (고쳐야 함)
1. **캐릭터 스케일 ~1.4배 작음.** 참고 주요 영웅 화면높이 ~35~40%(≈3.5유닛 @ortho5), 현재 ~25%(≈2.1유닛).
   원인 후보: 액터 Spine import 스케일(현재 `SkeletonDataAsset.CreateRuntimeInstance(...,0.01f)`)이 원본과
   다름. → 원본 액터 프리팹/스케일 확인해 맞출 것(추측 금지). 카메라 ortho 5.0은 원본값이므로 유지.
2. **영웅이 프레임에서 너무 아래.** 참고는 영웅이 화면 세로 중앙대(뒤로 마을), 현재는 하단에 몰리고 위쪽
   빈 바닥 과다. → 카메라 Y 또는 액터 기준 Y 정렬 확인(원본: 카메라 (0,0), 스테이션 y -1.0~-3.68).
3. **스킬 포커스 카메라 없음.** 참고는 스킬/궁극 시전 시 **배경 디밍 + 시전자 줌인**(프레임 26·30·33·34·48·54s
   에서 화면이 어두워지고 캐릭터에 포커스). 현재는 정적. → 스킬 트리거 시 카메라 줌/디밍 연출 추가(후순위).

## 검증 기준
참고 r_44.0(기본 전투)와 우리 캡처를 `REF_VS_OURS.png`처럼 나란히 비교해 영웅 크기/세로위치/배경이
눈으로 일치할 것. 카메라 ortho 5.0·위치(0,0)·실제 스테이션 좌표는 원본값이므로 변경 금지; 조정 대상은
**액터 스케일(원본값으로)** 과 **세로 정렬**, 그리고 스킬 카메라 연출.
