local t={}
function t:OnHandle(e)
local e=assert(GlobalPB.decode("PRT_HERO_DRAW_DRAW_RESP",e));
if(GameInit.GetDebugLogProto())then
;
;
end
ModulesInit.ActHeroDrawMgr:synActHeroDrawDrawData(e)
return e
end
return t 
