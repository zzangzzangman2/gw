local t={}
function t:OnHandle(e)
local e=assert(GlobalPB.decode("PRT_PLAYER_CITY_INNER_UNLOCK_RESP",e));
if(GameInit.GetDebugLogProto())then
;
;
end
PlayerCityMgr:OnResInnerUnlock(e)
return e
end
return t 
