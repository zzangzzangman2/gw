local t={}
function t:OnHandle(e)
local e=assert(GlobalPB.decode("PRT_PLAYER_CITY_INNER_COMPONENT_LEVEL_RESP",e));
if(GameInit.GetDebugLogProto())then
;
;
end
PlayerCityMgr:OnResInnerLevel(e)
return e
end
return t 
