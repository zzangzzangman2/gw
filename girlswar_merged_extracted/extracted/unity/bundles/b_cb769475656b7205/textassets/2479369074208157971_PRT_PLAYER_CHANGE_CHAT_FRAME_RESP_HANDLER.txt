local t={}
function t:OnHandle(e)
local e=assert(GlobalPB.decode("PRT_PLAYER_CHANGE_CHAT_FRAME_RESP",e));
if(GameInit.GetDebugLogProto())then
;
;
end
ChatMgr:OnRespChangeChatFrame(e)
return e
end
return t 
